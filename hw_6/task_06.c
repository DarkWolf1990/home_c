#include <stdio.h>

int main(){
    int number_1=0, number_2=0, differential=0;
    scanf("%d %d",&number_1, &number_2);
    differential = (number_1 - number_2 );
    printf("%d", differential);
    return 0;
}